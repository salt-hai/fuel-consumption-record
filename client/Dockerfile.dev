FROM node:20-alpine

WORKDIR /app

# 安装 pnpm
RUN npm install -g pnpm

# 复制依赖文件
COPY package.json pnpm-lock.yaml* ./

# 安装依赖
RUN pnpm install

# 开发时挂载源码目录，CMD 由 docker-compose 覆盖
EXPOSE 5173

CMD ["pnpm", "dev", "--host"]
